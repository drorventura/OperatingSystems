#include "defs.h"
#include "user.h"


int
main(int argc, char *argv[])
{
  int minPos = 0;
  int maxPos = 0;;
  char *pathToAdd = "";

  while( argv[0][mainPos] != "\0" )
  {



  }

  for(i = 1; i < argc; i++)
    printf(1, "%s%s", argv[i], i+1 < argc ? " " : "\n");
  exit();
}
